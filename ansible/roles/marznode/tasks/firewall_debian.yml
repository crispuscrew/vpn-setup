# Debian has no host firewall configured by default. Use ufw, and allow SSH FIRST
# so enabling the firewall can never lock us out. Then the Reality port; the gRPC
# port from the panel is opened later, in remote_prep.yml, for remote nodes only.
- name: Install ufw
  ansible.builtin.apt:
    name: ufw
    state: present
    update_cache: true

- name: Allow SSH before enabling the firewall
  community.general.ufw:
    rule: allow
    port: "22"
    proto: tcp

- name: Allow the Reality port
  community.general.ufw:
    rule: allow
    port: "{{ reality_port }}"
    proto: tcp

- name: Allow the Hysteria2 port (UDP)
  community.general.ufw:
    rule: allow
    port: "{{ hysteria2_port }}"
    proto: udp
  when: hysteria2_enabled | default(false) | bool

- name: Allow the TUIC port (UDP)
  community.general.ufw:
    rule: allow
    port: "{{ tuic_port }}"
    proto: udp
  when: tuic_enabled | default(false) | bool

- name: Allow the Shadowsocks port (TCP+UDP)
  community.general.ufw:
    rule: allow
    port: "{{ shadowsocks_port }}"
    proto: "{{ item }}"
  loop: [tcp, udp]
  when: shadowsocks_enabled | default(false) | bool

- name: Allow the Trojan port (TCP)
  community.general.ufw:
    rule: allow
    port: "{{ trojan_port }}"
    proto: tcp
  when: trojan_enabled | default(false) | bool

- name: Enable ufw (default deny incoming, allow outgoing)
  community.general.ufw:
    state: enabled
    policy: deny
    direction: incoming
